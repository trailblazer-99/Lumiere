<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LumiereMediaPlayer.Pages.StreamingMusicPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:LumiereMediaPlayer.Models.Streaming"
    xmlns:services="using:LumiereMediaPlayer.Services.Streaming"
    xmlns:helpers="using:LumiereMediaPlayer.Helpers"
    Loaded="OnPageLoaded">

    <Grid x:Name="PageContent" Margin="{StaticResource PageContentMargin}" Opacity="1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" Spacing="14">
                <FontIcon Glyph="&#xE8D6;" FontSize="32"/>
                <TextBlock Text="Music Streaming" Style="{StaticResource PageTitleTextStyle}"/>
            </StackPanel>
            <TextBlock Text="Search and discover popular music, then listen on your favorite platform."
                       Style="{StaticResource PageSubtitleTextStyle}"
                       Opacity="0.7"/>
        </StackPanel>

        <Pivot x:Name="MainPivot" Grid.Row="1" SelectionChanged="MainPivot_SelectionChanged">
            <PivotItem Header="Discover">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,16" Orientation="Horizontal" Spacing="12">
                        <TextBox x:Name="SearchBox" PlaceholderText="Search artists, songs, playlists..." KeyDown="OnSearchKeyDown" Width="240" VerticalAlignment="Bottom" />
                        <ComboBox x:Name="FilterComboBox" ItemsSource="{x:Bind ViewModel.SearchFilters, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SearchFilter, Mode=TwoWay}" Width="130" VerticalAlignment="Bottom" />
                        <Button Content="Search" Click="OnSearchButtonClick" VerticalAlignment="Bottom" Style="{StaticResource AccentButtonStyle}" />
                    </StackPanel>

                    <GridView x:Name="MusicGridView" Grid.Row="1" ItemsSource="{x:Bind ViewModel.Tracks, Mode=OneWay}" SelectionMode="None" IsItemClickEnabled="True" ItemClick="OnTrackClicked">
                        <GridView.ItemContainerStyle>
                            <Style TargetType="GridViewItem">
                                <Setter Property="Margin" Value="0"/>
                                <Setter Property="Padding" Value="12"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                            </Style>
                        </GridView.ItemContainerStyle>
                        <GridView.ItemTemplate>
                            <DataTemplate x:DataType="models:MusicApiTrack">
                                    <Border Width="200" Height="200" CornerRadius="8" Translation="0,0,8"
                                           Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                           BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                           PointerEntered="Card_PointerEntered" PointerExited="Card_PointerExited">
                                        <Border.Shadow>
                                            <ThemeShadow />
                                        </Border.Shadow>
                                        <Grid>
                                             <Image Source="{Binding HighResArtworkUrl}" Stretch="UniformToFill" />
                                             <Border x:Name="HoverOverlay" Opacity="0.0" CornerRadius="8">
                                                 <Border.Background>
                                                     <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                         <GradientStop Color="#00000000" Offset="0.0"/>
                                                         <GradientStop Color="#90000000" Offset="1.0"/>
                                                     </LinearGradientBrush>
                                                 </Border.Background>
                                                 <Grid>
                                                     <Border x:Name="PlayButtonIcon" Width="48" Height="48" CornerRadius="24" Background="#33FFFFFF" BorderBrush="#66FFFFFF" BorderThickness="1.5" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                                                         <Border.RenderTransform>
                                                             <ScaleTransform ScaleX="0.6" ScaleY="0.6" />
                                                         </Border.RenderTransform>
                                                         <FontIcon FontSize="20" Foreground="White" Glyph="&#xE768;" Margin="3,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                     </Border>
                                                 </Grid>
                                             </Border>
                                             <Border VerticalAlignment="Bottom" Height="80">
                                                 <Border.Background>
                                                     <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                         <GradientStop Color="Transparent" Offset="0"/>
                                                         <GradientStop Color="#CC000000" Offset="1"/>
                                                     </LinearGradientBrush>
                                                 </Border.Background>
                                                 <StackPanel VerticalAlignment="Bottom" Padding="12">
                                                     <TextBlock Text="{Binding TrackName}" Foreground="White" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                                     <TextBlock Text="{Binding DisplaySubtitle}" Foreground="#DDDDDD" FontSize="12" />
                                                 </StackPanel>
                                             </Border>
                                             <Border HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8" Padding="6,2" CornerRadius="4" Background="#CC000000" BorderThickness="1" BorderBrush="#33FFFFFF">
                                                 <TextBlock Text="{Binding ResultType}" Foreground="White" FontSize="10" FontWeight="SemiBold" />
                                             </Border>
                                             <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8" Spacing="4">
                                                 <HyperlinkButton NavigateUri="{Binding ExternalUrls.Spotify}" Visibility="{x:Bind helpers:VisibilityHelper.FromBoolean(HasSpotify), Mode=OneTime}" Width="32" Height="32" CornerRadius="16" Background="#CC000000" Padding="0" ToolTipService.ToolTip="Listen on Spotify">
                                                     <Image Width="24" Height="24" Stretch="Uniform" Source="https://www.google.com/s2/favicons?domain=spotify.com&amp;sz=128" />
                                                 </HyperlinkButton>
                                                 <HyperlinkButton NavigateUri="{Binding ExternalUrls.AppleMusic}" Visibility="{x:Bind helpers:VisibilityHelper.FromBoolean(HasAppleMusic), Mode=OneTime}" Width="32" Height="32" CornerRadius="16" Background="#CC000000" Padding="0" ToolTipService.ToolTip="Listen on Apple Music">
                                                     <FontIcon Glyph="&#xE93C;" FontSize="16" Foreground="#FA243C" />
                                                 </HyperlinkButton>
                                                 <HyperlinkButton NavigateUri="{Binding ExternalUrls.Tidal}" Visibility="{x:Bind helpers:VisibilityHelper.FromBoolean(HasTidal), Mode=OneTime}" Width="32" Height="32" CornerRadius="16" Background="#CC000000" Padding="0" ToolTipService.ToolTip="Listen on Tidal">
                                                     <TextBlock Text="T" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                 </HyperlinkButton>
                                             </StackPanel>
                                         </Grid>
                                     </Border>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>
                </Grid>
            </PivotItem>
            <PivotItem Header="Library">
                <GridView x:Name="LibraryGridView" IsItemClickEnabled="True" ItemClick="LibraryGridView_ItemClick" SelectionMode="None">
                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="services:SavedStreamingItem">
                            <Border Width="200" Height="200" Margin="0,0,16,16" CornerRadius="8" Translation="0,0,8"
                                  Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                  BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                  PointerEntered="Card_PointerEntered" PointerExited="Card_PointerExited">
                                <Border.Shadow>
                                    <ThemeShadow />
                                </Border.Shadow>
                                <Grid>
                                    <Image Source="{Binding PosterUrl}" Stretch="UniformToFill" />
                                    <Border x:Name="HoverOverlay" Opacity="0.0" CornerRadius="8">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="#00000000" Offset="0.0"/>
                                                <GradientStop Color="#90000000" Offset="1.0"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <Grid>
                                            <Border Width="48" Height="48" CornerRadius="24" Background="#33FFFFFF" BorderBrush="#66FFFFFF" BorderThickness="1.5" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <FontIcon FontSize="20" Foreground="White" Glyph="&#xE768;" Margin="3,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Border>
                                        </Grid>
                                     </Border>
                                     <Border VerticalAlignment="Bottom" Height="80">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="Transparent" Offset="0"/>
                                                <GradientStop Color="#CC000000" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <StackPanel VerticalAlignment="Bottom" Padding="12">
                                            <TextBlock Text="{Binding Title}" Foreground="White" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding Subtitle}" Foreground="#DDDDDD" FontSize="12" />
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>



